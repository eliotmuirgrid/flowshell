command:change() {
    local comment="$*"

    if [[ -z "$comment" ]]; then
        echo "Usage: change:change <comment>"
        return 1
    fi

    cd ~/command && git add . && git commit -m "$comment" && git push
}
