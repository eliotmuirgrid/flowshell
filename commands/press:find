press:find() {
   arg:check 1 "$@" || return;

   local result="$(dir:change:temp ~/press file:find $1)"
   echo $result 
   typeset -a list
   set -A list -- $result
   for item in "${list[@]}"; do
      press:open $item 
   done
}
