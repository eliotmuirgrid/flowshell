git:sub:add() {
    arg:check 1 "$@" || return
    local repo="$1"

    local dir
    dir=$(basename "$repo")
    dir="${dir%.git}"

    git submodule add "$repo" "$dir"
    git add .gitmodules "$dir"
    git commit -m "Added submodule $dir"
    git:push:all
}
