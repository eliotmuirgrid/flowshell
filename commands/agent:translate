agent:translate() {
   if [[ -z "$1" ]] || [[ -z "$2" ]]; then
      echo "Usage: agent:translate <file> <language>"
      return 1
   fi

   agent:file chatgpt $1 "Translate to $2 - just the translation - no commentary"
}
