voice:download() {
   arg:check 2 "$@" || return

   local dir="$1"
   local file="$2"

   echo "Downloading $file..."

   curl -L -f -O "$BASE/$dir/$file.onnx"
   curl -L -f -O "$BASE/$dir/$file.onnx.json"
}
