press:open() {
    local file="${1:-i}"
    local md_path

    md_path="$(md:path "$file")"
    md_path="$(filter:trim:begin "$md_path" "/press")"

    url:open "http://github.com/eliotmuirgrid/press/blob/master${md_path}"
}
