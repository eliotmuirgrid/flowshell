command:edit() {
    local arg1="$1"

    if [[ -z "$arg1" ]]; then
        echo "Usage: command:edit <arg1>"
        return 1
    fi
    vi $CMD_DIR/$arg1
    source $CMD_DIR/$arg1 
    command:change "Edited $arg1"
}
