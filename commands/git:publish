git:publish() {
   local msg="${*:-Update}"
   local root
   local root = ${pwd:top}
   echo "$root" 
   git add . || return
   git commit -m "$msg" || return

   local branch
   branch=$(git branch --show-current)

   git push -u origin "$branch"
}
