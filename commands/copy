copy() {
    local source="$1"
    local destination="$2"

    if [[ -z "$arg1" ]] || [[ -z "$arg2" ]]; then
        echo "Usage: copy <source> <destination>"
        return 1
    fi
    cp $source $destination
}
