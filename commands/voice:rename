voice:rename() {
   arg:check 2 "$@" || return;

   local from="$HOME/piper-voices/$1.onnx"
   local   to="$HOME/piper-voices/$2.onnx"

   file:move "$from"      "$to"
   file:move "$from.json" "$to.json"
}
