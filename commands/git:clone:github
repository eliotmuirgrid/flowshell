git:clone:github() {
   arg:check 2 "$@" || return;

   local repo="$1"
   local dest="$2"

   git:clone "$(git:url:github:eliotmuirgrid $repo)" "$dest"
   cd $dest
   git submodule update --init --recursive
}
