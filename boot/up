# Super important to know where you are in the file system
PROMPT='%m:%~ %# '
CMD_DIR=$HOME/command/commands

# Load custom shell commands
for f in $CMD_DIR/*; do
  #echo "Install $f"
  source "$f"
done

# Get VIM to use custom commands
echo "set runtimepath^=$HOME/command/vim" > $HOME/.vimrc
