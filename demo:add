demo:add() {
    local arg1="$1"
    local arg2="$2"

    if [[ -z "$arg1" ]] || [[ -z "$arg2" ]]; then
        echo "Usage: demo:add <arg1> <arg2>"
        return 1
    fi

    echo "arg1: $arg1"
    echo "arg2: $arg2"
}
